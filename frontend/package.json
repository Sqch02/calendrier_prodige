{
  "name": "calendar-app-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.5.1",
    "date-fns": "^2.30.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "NODE_ENV=production DISABLE_ESLINT_PLUGIN=true CI=false react-scripts build",
    "build:legacy": "NODE_OPTIONS=--openssl-legacy-provider DISABLE_ESLINT_PLUGIN=true CI=false react-scripts build",
    "build:docker": "node fix-ajv.js && NODE_OPTIONS=--openssl-legacy-provider DISABLE_ESLINT_PLUGIN=true CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "overrides": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "ajv-keywords": "^5.1.0",
    "typescript": "^5.0.3",
    "semver": "^7.5.3",
    "@babel/core": "^7.22.0",
    "@babel/runtime": "^7.22.0"
  },
  "resolutions": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "ajv-keywords": "^5.1.0"
  },
  "proxy": "http://localhost:5000"
}