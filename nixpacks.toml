[phases.setup]
nixPkgs = ["nodejs", "nodejs.pkgs.pnpm", "yarn"]

[phases.install]
cmds = ["npm config set legacy-peer-deps true", "npm install", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "npm start"
